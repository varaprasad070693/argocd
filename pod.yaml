apiVersion: v1
kind : Pod
metadata:
  name: sonar-qube-argo-pod
  labels:
    app: sonar
spec:
  containers:
    - name: sonar-qube
      image: sonarqube:9.9-community
      ports:
      - containerPort: 9000
